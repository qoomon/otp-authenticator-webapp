name: Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:        
    - uses: actions/checkout@v2
    - name: Add GitHub Deploy Key
      run: |
        echo "${{ secrets.GITHUB_DEPLOY_KEY }}" > ~/.ssh/github
        chmod 600 ~/.ssh/github
    
    - name: Setup Node.js
      uses: actions/setup-node@v1
      with:
        node-version: 12.x
        
    - name: npm install, build, and test
      run: |
        npm ci
        npm run build
      env:
        CI: true
        
    - name: Deploy to GitHub Pages
      run: |
        eval $(ssh-agent -s) && ssh-add ~/.ssh/github
        ./deploy_to_github_pages.sh 'dist' "git@github.com:${{ github.repository }}.git" 'gh-pages' 'GitHub Action'
        
    